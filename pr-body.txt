## Summary

- **Three-panel inbox shell** — sidebar, email list, reading pane with system `prefers-color-scheme` theme detection and fade-in animations
- **IMAP service** — connection pool (`src/services/imap.js`) with fetch, read/unread, archive, and delete; full message parsing via `mailparser`
- **Email list** — sender display name (falls back to address), unread dot indicator + bold text, 20-per-page pagination with prev/next controls
- **Reading pane** — HTML email in sandboxed iframe; remote images blocked by default with per-message toggle and a settings option for auto-loading
- **Account management** — IMAP/SMTP add modal with Gmail, Outlook, and iCloud provider logos; AES-256-GCM password encryption
- **Settings modal** — theme cycling (dark / light / system), remote image preference stored in localStorage
- **Auth page** — login/register/recover with system color-scheme detection and slide-up animation

## Bug fixes

| Bug | Fix |
|---|---|
| "Mark as unread" was marking as read | `nowRead` logic was inverted; `_current.data.unread` now updated after auto-read on open |
| "Show images" cleared the message | Button wired programmatically after render; `iframe.onload` set before `srcdoc` |
| External images blocked even after showing | Helmet CSP `img-src` lacked `https:`; added `https:` and `http:` |
| Compose shortcut `c` typed into recipient field | Added `e.preventDefault()` to all shortcut cases |
| Modals dark in light mode | Replaced hardcoded `#0e0e0e` with `var(--bg-2)` |
| Archive/delete gave no feedback | Added confirm dialog before action and toast on success |

## Reviewer notes

- Requires Node >= 22.5 for `node:sqlite` built-in
- `.env` must be present (see `.env.example`); first run auto-creates `data/spindle.db`
- Run `node scripts/seed-invite.js` to generate the first invite code before registering
- `data/sessions/` and `data/*.txt` added to `.gitignore`

Generated with [Claude Code](https://claude.ai/claude-code)
